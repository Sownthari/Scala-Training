
# Security Filters
play.http.filters = "security.Filters"
play.server.http.port = 9001
play.modules.enabled += "modules.Module"

# ===============================================
# CORS Configuration
# ===============================================
play.filters.enabled += "play.filters.cors.CORSFilter"

play.filters.cors {
  pathPrefixes = ["/api"]           # Enable CORS for all API paths
  allowedOrigins = ["*"]            # Allow any origin
  allowedHttpMethods = ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
  allowedHttpHeaders = ["Accept", "Origin", "Content-Type", "Authorization", "X-Auth-Token"]
  preflightMaxAge = 3 days
}

# --- DB config (MySQL via HikariCP) ---
slick.dbs.default.profile = "slick.jdbc.MySQLProfile$"
slick.dbs.default.db.driver = "com.mysql.cj.jdbc.Driver"
slick.dbs.default.db.url = "jdbc:mysql://azuremysql8823.mysql.database.azure.com:3306/sownthari?useSSL=false&serverTimezone=UTC&connectionTimeZone=UTC"
slick.dbs.default.db.user = "mysqladmin"
slick.dbs.default.db.password = "Password@12345"

# --- JWT config ---
auth.jwt.secret = "b79e18e53debd30b0ce6d812b689d9a93f419c46f0b243a9cdd0c8e2ad7f973b"
auth.jwt.issuer = "meeting-room-system"
auth.jwt.expirationMinutes = 60

# --- Kafka config (simple) ---
kafka.topic.notifications = "reservation-notifications"

# This secret is used for session encryption and CSRF protection.
play.http.secret.key = "b0f4f35a4f9c4e8a9e8d1c7f3a8b2c5d7f9e1a3c4b6d8f0e2a4c6e8f0a2b4c"




